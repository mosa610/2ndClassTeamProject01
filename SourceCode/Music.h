#pragma once

#include <iostream>
#include "../GameLib/game_lib.h"

enum SE
{
    SELECT = 10,
    ALARM,
    EXPLOSION,
    DRIL,
    HUMMER,
    FIRER,
    CHAINSOW,
    AX,
    RAIN,
    THUNDER,
    COLLAPSE
};

enum BGM
{
    TITLE,
    GAME,
    CLEAR,
    OVER,
    RESULT
};