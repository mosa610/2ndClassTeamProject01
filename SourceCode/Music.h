#pragma once

#include <iostream>
#include "../GameLib/game_lib.h"

enum SE
{
    SELECT = 10,
    CANCEL,
    ALARM,
    EXPLOSION,
    COLLAPSE
};

enum BGM
{
    TITLE,
    GAME,
    CLEAR,
    OVER,
    RESULT
};