#pragma once

#include <iostream>
#include "../GameLib/game_lib.h"

enum SE
{
    SELECT = 10,
    ALARM,
    EXPLOSION,
    DRILL,
    HUMMER,
    FIRE,
    CHAINSOW,
    AXE,
    RAIN,
    THUNDER,
    COLLAPSE
};

enum BGM
{
    TITLE,
    GAME,
    CLEAR,
    OVER,
    RESULT
};